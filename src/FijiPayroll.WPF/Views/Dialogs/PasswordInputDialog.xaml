<Window x:Class="FijiPayroll.WPF.Views.Dialogs.PasswordInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reset Password"
        Width="420" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <ResourceDictionary Source="../../Styles/FluentStyles.xaml" />
    </Window.Resources>

    <Border CornerRadius="10" Background="{DynamicResource BrushBgPanel}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="Reset User Password"
                       FontSize="18" FontWeight="Bold"
                       Foreground="{DynamicResource BrushTextPrimary}" Margin="0,0,0,8"/>

            <!-- Instructions -->
            <TextBlock x:Name="InstructionsTextBlock" Grid.Row="1"
                       Text="Enter a new temporary password:"
                       FontSize="13" Foreground="{DynamicResource BrushTextSecondary}"
                       TextWrapping="Wrap" Margin="0,0,0,14"/>

            <!-- Password Input -->
            <PasswordBox x:Name="NewPasswordBox" Grid.Row="2"
                         Height="38"
                         Background="{DynamicResource BrushBgDeep}"
                         Foreground="{DynamicResource BrushTextPrimary}"
                         BorderBrush="{DynamicResource BrushBorder}"
                         BorderThickness="1"
                         Padding="10,8"
                         FontSize="14"
                         Margin="0,0,0,16">
                <PasswordBox.Template>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </PasswordBox.Template>
            </PasswordBox>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Click="CancelButton_Click"
                        Width="90" Height="34"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,10,0" />
                <Button Content="Reset Password"
                        Click="OkButton_Click"
                        Width="120" Height="34"
                        Style="{StaticResource ModernButton}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
