<Window x:Class="FijiPayroll.WPF.Views.Dialogs.PasswordInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reset Password"
        Width="420" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="10" Background="#1B2032" BorderBrush="#2D3548" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="Reset User Password"
                       FontSize="18" FontWeight="Bold"
                       Foreground="#FFFFFF" Margin="0,0,0,8"/>

            <!-- Instructions -->
            <TextBlock x:Name="InstructionsTextBlock" Grid.Row="1"
                       Text="Enter a new temporary password:"
                       FontSize="13" Foreground="#A0AEC0"
                       TextWrapping="Wrap" Margin="0,0,0,14"/>

            <!-- Password Input -->
            <PasswordBox x:Name="NewPasswordBox" Grid.Row="2"
                         Height="38"
                         Background="#111827"
                         Foreground="#FFFFFF"
                         BorderBrush="#2D3548"
                         BorderThickness="1"
                         Padding="10,8"
                         FontSize="14"
                         Margin="0,0,0,16">
                <PasswordBox.Template>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#0D99FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </PasswordBox.Template>
            </PasswordBox>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Click="CancelButton_Click"
                        Width="90" Height="34"
                        Background="#2E3656"
                        Foreground="#A0AEC0"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,0,10,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="Reset Password"
                        Click="OkButton_Click"
                        Width="120" Height="34"
                        Background="#0D99FF"
                        Foreground="#FFFFFF"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
