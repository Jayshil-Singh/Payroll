<UserControl x:Class="FijiPayroll.WPF.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FijiPayroll.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" FontFamily="Segoe UI Semibold" />
            <TextBlock Text="Generate statutory audit reports, Westpac/BSP bank file exports, and FRCS/FNPF compliance documents." FontSize="14" Foreground="#A0AEC0" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Main Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Card 1: Bank Export file -->
            <Border Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" CornerRadius="8" Background="#1B2032" BorderBrush="#2E3656" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="Bank Disbursement Files" FontSize="16" Foreground="#FFFFFF" FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBlock Text="Export net pay electronic transfer files conforming to Fiji clearing house requirements." FontSize="12" Foreground="#A0AEC0" TextWrapping="Wrap" Height="40" />
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Border CornerRadius="4" BorderBrush="#2D3748" BorderThickness="1" Background="#0F111A" Width="160" Height="32" Padding="8,4" VerticalAlignment="Center">
                            <TextBlock Text="Select Westpac / BSP" Foreground="#FFFFFF" VerticalAlignment="Center" FontSize="12"/>
                        </Border>
                        <Button Content="Generate Export" Margin="12,0,0,0" Width="120" Height="32" Background="#0D99FF" Foreground="#FFFFFF" FontWeight="SemiBold" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Card 2: FNPF Contribution -->
            <Border Grid.Row="0" Grid.Column="1" Margin="12,0,0,12" CornerRadius="8" Background="#1B2032" BorderBrush="#2E3656" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="FNPF Contribution Returns" FontSize="16" Foreground="#FFFFFF" FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBlock Text="Generate FNPF monthly CSV remittance file containing calculated employee (8%) and employer (10%) portions." FontSize="12" Foreground="#A0AEC0" TextWrapping="Wrap" Height="40" />
                    <Button Content="Download Remittance CSV" Margin="0,16,0,0" Width="180" Height="32" Background="#2E3656" Foreground="#FFFFFF" FontWeight="SemiBold" BorderBrush="#4A5568" BorderThickness="1" HorizontalAlignment="Left">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Card 3: FRCS Tax Return -->
            <Border Grid.Row="1" Grid.Column="0" Margin="0,12,12,0" CornerRadius="8" Background="#1B2032" BorderBrush="#2E3656" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="FRCS PAYE Returns" FontSize="16" Foreground="#FFFFFF" FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBlock Text="Generate monthly EMS (Employer Monthly Summary) data sheets reporting PAYE tax deductions to Fiji Revenue Services." FontSize="12" Foreground="#A0AEC0" TextWrapping="Wrap" Height="40" />
                    <Button Content="Generate EMS Return" Margin="0,16,0,0" Width="160" Height="32" Background="#2E3656" Foreground="#FFFFFF" FontWeight="SemiBold" BorderBrush="#4A5568" BorderThickness="1" HorizontalAlignment="Left">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Card 4: Historical Payslips -->
            <Border Grid.Row="1" Grid.Column="1" Margin="12,12,0,0" CornerRadius="8" Background="#1B2032" BorderBrush="#2E3656" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="Employee Payslip Portal" FontSize="16" Foreground="#FFFFFF" FontWeight="Bold" Margin="0,0,0,8" />
                    <TextBlock Text="Review, sign, print, or batch email PDF payslips to employees for any past calculated payroll runs." FontSize="12" Foreground="#A0AEC0" TextWrapping="Wrap" Height="40" />
                    <Button Content="Batch Email Payslips" Margin="0,16,0,0" Width="160" Height="32" Background="#38B2AC" Foreground="#FFFFFF" FontWeight="SemiBold" BorderThickness="0" HorizontalAlignment="Left">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
