<UserControl x:Class="FijiPayroll.WPF.Views.DiagnosticsDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FijiPayroll.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="{Binding Title}" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource BrushTextPrimary}" FontFamily="Segoe UI Semibold" />
            <TextBlock Text="Real-time latency metrics, thread pool utilization, background task queues, and DB responsiveness watchdog." FontSize="13" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Metrics Workspace Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Card 1: System Health score -->
            <Border Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" CornerRadius="8" Background="{DynamicResource BrushBgPanel}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Padding="20">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="SYSTEM HEALTH SCORE" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" FontWeight="Bold" HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding HealthScore}" FontSize="72" Foreground="#38B2AC" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10" />
                    <TextBlock Text="All engines operational" FontSize="13" Foreground="#38B2AC" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Card 2: Memory & CPU info -->
            <Border Grid.Row="0" Grid.Column="1" Margin="12,0,0,12" CornerRadius="8" Background="{DynamicResource BrushBgPanel}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="Resource Utilization" FontSize="16" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" Margin="0,0,0,15" />
                    
                    <TextBlock Text="Memory In-Use (Working Set)" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" />
                    <TextBlock Text="{Binding MemoryUsedMb, StringFormat='{}{0} MB'}" FontSize="22" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" Margin="0,4,0,15" />

                    <TextBlock Text="Estimated CPU Usage" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" />
                    <TextBlock Text="{Binding CpuUsagePercent, StringFormat='{}{0}%'}" FontSize="22" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <!-- Card 3: Database Latencies -->
            <Border Grid.Row="1" Grid.Column="0" Margin="0,12,12,0" CornerRadius="8" Background="{DynamicResource BrushBgPanel}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="Database Latency Watchdog" FontSize="16" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" Margin="0,0,0,15" />
                    
                    <TextBlock Text="EF Core Query Latency" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" />
                    <TextBlock Text="{Binding DatabaseLatencyMs, StringFormat='{}{0} ms'}" FontSize="32" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" Margin="0,6,0,15" />

                    <TextBlock Text="Responsiveness Status: Excellent" FontSize="13" Foreground="#38B2AC" />
                </StackPanel>
            </Border>

            <!-- Card 4: Threads and Job queue depth -->
            <Border Grid.Row="1" Grid.Column="1" Margin="12,12,0,0" CornerRadius="8" Background="{DynamicResource BrushBgPanel}" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1" Padding="20">
                <StackPanel>
                    <TextBlock Text="Task Processing Queues" FontSize="16" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" Margin="0,0,0,15" />
                    
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Busy Worker Threads:" Foreground="{DynamicResource BrushTextSecondary}" FontSize="12" />
                        <TextBlock Grid.Column="1" Text="{Binding BusyWorkerThreads}" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" FontSize="12" />
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Active Worker Threads:" Foreground="{DynamicResource BrushTextSecondary}" FontSize="12" />
                        <TextBlock Grid.Column="1" Text="{Binding ActiveWorkerThreads}" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" FontSize="12" />
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Pending Compliance Jobs:" Foreground="{DynamicResource BrushTextSecondary}" FontSize="12" />
                        <TextBlock Grid.Column="1" Text="{Binding PendingJobsCount}" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" FontSize="12" />
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Pending Outbound Alerts:" Foreground="{DynamicResource BrushTextSecondary}" FontSize="12" />
                        <TextBlock Grid.Column="1" Text="{Binding PendingNotificationsCount}" Foreground="{DynamicResource BrushTextPrimary}" FontWeight="Bold" FontSize="12" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
