<UserControl x:Class="FijiPayroll.WPF.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- Section Header Style -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource BrushAccent}" />
            <Setter Property="Margin"     Value="0,0,0,12" />
        </Style>

        <!-- Field Label Style -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="12" />
            <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}" />
            <Setter Property="Margin"     Value="0,0,0,4" />
        </Style>

        <!-- Input TextBox Style -->
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Background"       Value="{DynamicResource BrushBgDeep}" />
            <Setter Property="Foreground"       Value="{DynamicResource BrushTextPrimary}" />
            <Setter Property="BorderBrush"      Value="{DynamicResource BrushBorder}" />
            <Setter Property="BorderThickness"  Value="1" />
            <Setter Property="Padding"          Value="10,8" />
            <Setter Property="FontSize"         Value="13" />
            <Setter Property="Height"           Value="36" />
            <Setter Property="CaretBrush"       Value="{DynamicResource BrushAccent}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Loading overlay -->
        <Border Background="#CCFFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"
                Panel.ZIndex="50">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                             Background="Transparent"
                             Foreground="{DynamicResource BrushAccent}"
                             BorderThickness="0"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource BrushTextSecondary}"
                           FontSize="13" HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="System Settings" FontSize="28" FontWeight="Bold"
                                   Foreground="{DynamicResource BrushTextPrimary}" FontFamily="Segoe UI Semibold"/>
                        <TextBlock Text="Configure payroll defaults, system directories, and SMTP email delivery"
                                   FontSize="14" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Command="{Binding LoadCommand}"
                                Content="&#xE72C; Reload"
                                FontFamily="Segoe MDL2 Assets, Segoe UI"
                                Style="{StaticResource SecondaryButton}"
                                Width="90" Margin="0,0,10,0"/>
                        <Button Command="{Binding SaveCommand}"
                                Content="&#xE74E; Save"
                                FontFamily="Segoe MDL2 Assets, Segoe UI"
                                Style="{StaticResource ModernButton}"
                                Width="90"/>
                    </StackPanel>
                </Grid>

                <!-- Status Bar -->
                <Border Grid.Row="1" CornerRadius="6" Margin="0,0,0,16"
                        Padding="12,8"
                        Background="{DynamicResource BrushBgDeep}"
                        BorderThickness="1">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="{DynamicResource BrushAlertGreen}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasError}" Value="True">
                                    <Setter Property="BorderBrush" Value="{DynamicResource BrushAlertRed}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsDirty}" Value="True">
                                    <Setter Property="BorderBrush" Value="{DynamicResource BrushAlertOrange}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="&#xE946;" FontFamily="Segoe MDL2 Assets"
                                   Foreground="{DynamicResource BrushAccent}" FontSize="14"
                                   VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="1" Text="{Binding StatusMessage}"
                                   Foreground="{DynamicResource BrushTextSecondary}"
                                   FontSize="12" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="Unsaved changes" FontSize="11"
                                   Foreground="#ED8936" FontWeight="SemiBold" VerticalAlignment="Center"
                                   Visibility="{Binding IsDirty, Converter={StaticResource BoolToVis}}"/>
                    </Grid>
                </Border>

                <!-- Settings Columns -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEFT COLUMN -->
                    <StackPanel Grid.Column="0">

                        <!-- Payroll Defaults Section -->
                        <Border CornerRadius="8" Background="{DynamicResource BrushBgPanel}"
                                BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1"
                                Padding="20" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="&#xE8A5;  Payroll Defaults"
                                           FontFamily="Segoe MDL2 Assets, Segoe UI"
                                           Style="{StaticResource SectionHeader}"/>

                                <TextBlock Text="Default Pay Frequency" Style="{StaticResource FieldLabel}"/>
                                <ComboBox SelectedItem="{Binding DefaultPayFrequency, Mode=TwoWay}"
                                          Height="36" Margin="0,0,0,14"
                                          Background="{DynamicResource BrushBgDeep}"
                                          Foreground="{DynamicResource BrushTextPrimary}"
                                          BorderBrush="{DynamicResource BrushBorder}">
                                    <ComboBoxItem>Weekly</ComboBoxItem>
                                    <ComboBoxItem>Fortnightly</ComboBoxItem>
                                    <ComboBoxItem>Monthly</ComboBoxItem>
                                    <ComboBoxItem>Semi-Monthly</ComboBoxItem>
                                </ComboBox>

                                <TextBlock Text="Default Payroll Calendar" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding DefaultPayrollCalendar, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputBox}" Margin="0,0,0,14"/>

                                <TextBlock Text="Negative Net Pay Policy" Style="{StaticResource FieldLabel}"/>
                                <ComboBox SelectedItem="{Binding NegativePayPolicy, Mode=TwoWay}"
                                          Height="36" Margin="0,0,0,0"
                                          Background="{DynamicResource BrushBgDeep}"
                                          Foreground="{DynamicResource BrushTextPrimary}"
                                          BorderBrush="{DynamicResource BrushBorder}">
                                    <ComboBoxItem>PartialDeduction</ComboBoxItem>
                                    <ComboBoxItem>ZeroFloor</ComboBoxItem>
                                    <ComboBoxItem>CarryForward</ComboBoxItem>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <!-- Directory Paths Section -->
                        <Border CornerRadius="8" Background="{DynamicResource BrushBgPanel}"
                                BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1"
                                Padding="20" Margin="0,0,0,0">
                            <StackPanel>
                                <TextBlock Text="&#xE8B7;  System Directories"
                                           FontFamily="Segoe MDL2 Assets, Segoe UI"
                                           Style="{StaticResource SectionHeader}"/>

                                <TextBlock Text="Submissions Path" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding DefaultSubmissionPaths, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputBox}" Margin="0,0,0,14"/>

                                <TextBlock Text="Backup Directory" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding BackupDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputBox}" Margin="0,0,0,14"/>

                                <TextBlock Text="Export Directory" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding ExportDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputBox}" Margin="0,0,0,14"/>

                                <TextBlock Text="Import Directory" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding ImportDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource InputBox}" Margin="0,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Spacer -->
                    <Grid Grid.Column="1"/>

                    <!-- RIGHT COLUMN: SMTP -->
                    <Border Grid.Column="2" CornerRadius="8"
                            Background="{DynamicResource BrushBgPanel}"
                            BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1"
                            Padding="20" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="&#xE715;  SMTP Email Configuration"
                                       FontFamily="Segoe MDL2 Assets, Segoe UI"
                                       Style="{StaticResource SectionHeader}"/>

                            <TextBlock Text="The system uses these credentials to deliver pay slips, compliance notifications, and system alerts."
                                       FontSize="11" Foreground="{DynamicResource BrushTextSecondary}"
                                       TextWrapping="Wrap" Margin="0,0,0,16"/>

                            <TextBlock Text="SMTP Host" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding SmtpHost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource InputBox}" Margin="0,0,0,14"
                                     ToolTip="e.g. smtp.office365.com or smtp.gmail.com"/>

                            <TextBlock Text="SMTP Port" Style="{StaticResource FieldLabel}"/>
                            <TextBox Style="{StaticResource InputBox}" Margin="0,0,0,14"
                                     ToolTip="Common: 25, 465 (SSL), 587 (TLS)">
                                <TextBox.Text>
                                    <Binding Path="SmtpPort" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <ExceptionValidationRule/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>

                            <TextBlock Text="Username / Email" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding SmtpUsername, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource InputBox}" Margin="0,0,0,14"/>

                            <TextBlock Text="Password" Style="{StaticResource FieldLabel}"/>
                            <PasswordBox x:Name="SmtpPasswordBox"
                                         Background="{DynamicResource BrushBgDeep}"
                                         Foreground="{DynamicResource BrushTextPrimary}"
                                         BorderBrush="{DynamicResource BrushBorder}"
                                         BorderThickness="1"
                                         Padding="10,8"
                                         Height="36"
                                         Margin="0,0,0,14"
                                         PasswordChanged="SmtpPasswordBox_PasswordChanged">
                                <PasswordBox.Template>
                                    <ControlTemplate TargetType="PasswordBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="5">
                                            <ScrollViewer x:Name="PART_ContentHost"
                                                          Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsFocused" Value="True">
                                                <Setter Property="BorderBrush" Value="{DynamicResource BrushAccent}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </PasswordBox.Template>
                            </PasswordBox>

                            <CheckBox IsChecked="{Binding SmtpSslEnabled, Mode=TwoWay}"
                                      Content="Enable SSL/TLS Encryption"
                                      Foreground="{DynamicResource BrushTextPrimary}"
                                      FontSize="13" Margin="0,0,0,20"/>

                            <!-- Save Button -->
                            <Button Command="{Binding SaveCommand}"
                                    Content="&#xE74E;  Save Settings"
                                    FontFamily="Segoe MDL2 Assets, Segoe UI"
                                    Style="{StaticResource ModernButton}"
                                    HorizontalAlignment="Stretch"
                                    Height="38"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
